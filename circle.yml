deployment:  
  production:
    branch: 'master'
    commands:
      - '[[ ! -e "$(git rev-parse --git-dir)/shallow" ]] || git fetch --unshallow'
      - sh ./deploy.sh
dependencies:
    cache_directories:
      - 'node_modules'
test:
    override:
        - echo 'test'  